/* teste_valido.cminus
   Codigo C- sem erros lexicos (usa a BNF completa) */

int vetor[10];
int x;

void main(void)
{
    x = 0;
    while (x < 10) {
        vetor[x] = x * 2 + 1;
        x = x + 1;
    }
    if (x >= 10)
        return;
    else
        x = x - 1;
}
